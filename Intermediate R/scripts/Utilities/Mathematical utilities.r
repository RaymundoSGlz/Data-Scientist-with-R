# Calcula la suma de los valores absolutos redondeados
# de los errores de entrenamiento.
# Puedes trabajar en partes o con una sola línea de código.
# No es necesario almacenar el resultado en una variable,
# solo haz que R lo imprima.

# The errors vector has already been defined for you
errors <- c(1.9, -2.6, 4.0, -9.5, -3.4, 7.3)

# Sum of absolute rounded values of errors
sum(round(abs(errors)))
