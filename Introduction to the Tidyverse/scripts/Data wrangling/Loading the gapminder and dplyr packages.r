# Usa la función library() para cargar el paquete dplyr,
# tal como hemos cargado el paquete gapminder por ti.
# Escribe gapminder, en una línea aparte,
# para ver el conjunto de datos gapminder.

# Load the gapminder package
library(gapminder)

# Load the dplyr package
library(dplyr)

# Look at the gapminder dataset
gapminder
